/**
 * Un grupo de 5 personas va a sentarse en fila para una foto.
 * Calcule un algoritmo que me indique ¿Cuántas posiciones son posibles?
 * n=5
 * k=5,1
 */

const factorial = require("./factorial");

const ejercicio1 = () => {
  return factorial(5);
};

console.log(ejercicio1());
